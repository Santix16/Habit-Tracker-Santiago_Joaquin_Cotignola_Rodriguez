import { HabitFilterPipe } from './habit-filter-pipe';

describe('HabitFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HabitFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
